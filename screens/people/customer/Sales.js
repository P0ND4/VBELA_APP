import { View, Text } from "react-native";
import Layout from '@components/Layout';

const Sales = () => {
  return (
    <Layout>
      <Text>Sales</Text>
    </Layout>
  );
};

export default Sales;
