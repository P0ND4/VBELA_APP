import { View, Text } from 'react-native'
import Layout from '@components/Layout'

import theme from '@theme'

const ClientSupplier = () => {
  return (
    <Layout style={{ marginTop: 0 }}>
      <Text>ClientSupplier</Text>
    </Layout>
  )
}

export default ClientSupplier